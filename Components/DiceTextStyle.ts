import { StyleSheet } from 'react-native';

export const styles = StyleSheet.create({
  instruction: {
    fontSize: 24,
    fontWeight: '700',
    color: 'blue',
    letterSpacing: 0.3,
    textAlign: 'center',
  },  
});